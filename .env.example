# Environment Configuration Template for Lekker Bezig
# Copy this file to .env and update with your actual values

# Node.js Environment
NODE_ENV=development

# Google OAuth2 Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com
# 1. Create a new project or select existing
# 2. Enable Google+ API
# 3. Create OAuth 2.0 Client ID
# 4. Add authorized redirect URI: http://localhost:3001/auth/google/callback
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Session Configuration
# Generate a secure random string for production (use: openssl rand -base64 32)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# JWT Configuration
# Generate a secure random string for production (use: openssl rand -base64 64)
JWT_SECRET=your-jwt-secret-change-this-in-production

# Firebase Configuration
# Get these from Firebase Console: https://console.firebase.google.com
# 1. Create a new Firebase project
# 2. Go to Project Settings > Service accounts
# 3. Generate new private key (downloads JSON file)
# 4. Extract the values below from that JSON file
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_PRIVATE_KEY_ID=your-firebase-private-key-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour-private-key-here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=your-service-account-email@your-project.iam.gserviceaccount.com
FIREBASE_CLIENT_ID=your-firebase-client-id

# Server Configuration
PORT=3001

# Push Notification Configuration (VAPID Keys)
# Generate VAPID keys using: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key-here
VAPID_PRIVATE_KEY=your-vapid-private-key-here

# EmailJS Configuration
# Get these from EmailJS Dashboard: https://www.emailjs.com/
# 1. Create EmailJS account and service
# 2. Create email template
# 3. Get public key from Account settings
EMAILJS_PUBLIC_KEY=your-emailjs-public-key
EMAILJS_SERVICE_ID=your-emailjs-service-id
EMAILJS_TEMPLATE_ID=your-emailjs-template-id
EMAILJS_EMAIL_TO=your-email@the-experts.nl
EMAILJS_EMAIL_FROM=your-email@the-experts.nl
EMAILJS_NAME_OF_EXPERT=Your Name

# Production Domain (update for production deployment)
# PRODUCTION_DOMAIN=https://your-production-domain.com